import styled from 'styled-components';

const Button = styled.button`
  display: block;
	background: #00a884;
	color: #fff;
	font-weight: bold;
	padding: 10px 20px;
	border-radius: 5px;
	cursor: pointer;

	&:hover {
		opacity: 0.9;
	}
`;

export default Button;

